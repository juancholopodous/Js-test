// Funciones

// Declaración de la Función
suma() // Forma de mandar a llamar a la función

function suma () {
    console.log(10 + 5);
};

// Expresión de una Función
resta() // Forma de mandar a llamar a la función

const resta = function() {
    console.log(2 - 2)
};


// IIFE
(function() {
    console.log('Esto es una función')
}) ();  