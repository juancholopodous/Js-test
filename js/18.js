function suma (n1 = 0, n2 =0) { //numero1 y 2 son parametros
    console.log(n1 + n2);
}
suma(10, 5); // Argumentos o valores reales
suma(1, 3); // Argumentos o valores reales
suma(3); // Argumentos o valores reales
suma(12, 22); // Argumentos o valores reales


const resta = function(n1, n2) {
    console.log(n2 - n1)
}
resta(1, 1);
resta(3, 4);
resta(4, 1);